// Vercel Serverless Entry Point
// This file acts as the serverless function handler for all /api/* routes.
// It imports the Express app and exports it as a default handler for Vercel.

import app from '../server.js';

export default app;
